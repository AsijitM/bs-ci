# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build my-nx-workspace`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t my-nx-workspace`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system my-nx-workspace && \
          adduser --system -G my-nx-workspace my-nx-workspace

COPY apps/my-nx-workspace/dist my-nx-workspace/
RUN chown -R my-nx-workspace:my-nx-workspace .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix my-nx-workspace --omit=dev -f install

CMD [ "node", "my-nx-workspace" ]
